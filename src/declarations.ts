export interface AppPage {
  url: string;
  iosIcon: string;
  mdIcon: string;
  title: string;
  onClick?: () => void;
}
export interface IndicadoresPage {
  url: string;
  iosIcon: string;
  mdIcon: string;
  title: string;
}
